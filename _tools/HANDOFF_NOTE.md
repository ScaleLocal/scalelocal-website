# Start here

`README.md` in this folder explains the layout and the verification gates.

**The full handoff — mission, the from-scratch standard, deployment method, honesty
rules, outreach state and the traps — lives on Matt's machine at
`Documents\Claude\ScaleLocalCode\SiteBuilds\HANDOFF_SITE_BUILDS.md`,
alongside the research notes and pitch copy.**

Those are deliberately NOT in this repo: it feeds a public URL, and
`research/*.md`, `SIX_FIRM_AUDIT.md` and `THREE_PITCHES.md` contain prospect
intelligence and outreach copy that must not be published. `.vercelignore` keeps
this `_tools` directory out of the deployment regardless.

If `ScaleLocalCode\SiteBuilds\` is missing, everything here still works — but
re-read the research before writing any prospect-facing claim, because the
do-not-claim lists are the part that keeps the builds honest.
